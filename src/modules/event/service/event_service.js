import {
  define
} from 'src/containerHelper'
import _ from 'lodash'
module.exports = define('eventService', ({
}) => {
  const getEventConfig = async id => {
  }
  return {
    getEventConfig
  }
})
